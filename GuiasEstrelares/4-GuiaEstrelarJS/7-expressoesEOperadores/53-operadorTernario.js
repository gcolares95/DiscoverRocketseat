// Operador Condicional (Ternário)

// Dependendo da condição, nós receberemos valores diferentes 
 
// Condição então valor 1 se não valor 2
// condition ? value 1 : value2

// Exemplos

// Café da manhã top
let pao = false;
let queijo = false;

const niceBreakfast = pao && queijo ? 'Café top uhh' : 'Café ruim kk';

console.log(niceBreakfast);

// Maior de 18
let age = 16;

const podeDirigir = age >= 18 ? 'Pode dirigir fi' : 'Não pode dirigit ainda po'

console.log(podeDirigir);